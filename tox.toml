requires = ["tox>=4"]
env_list = ["testing"]

[env.testing]
description = "Run pytest and unittest"
deps = ["-rrequirements.txt"]
commands = [
    ["python", "-m", "pytest"],
    ["python", "-m", "unittest", "discover", "-s", "tests/unit"]
]